<%= tag.line id: dom_id(line),
             class: "pc-line",
             x1: line.source.box.x,
             y1: line.source.box.y,
             x2: line.destination.box.x,
             y2: line.destination.box.y,
             data: { controller: "line",
                     line_target: "line",
                     line_source_id_value: dom_id(line.source),
                     line_destination_id_value: dom_id(line.destination) } %>
